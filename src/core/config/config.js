const dbConfig = {
    development: {
        host: "localhost",
        user: "root",
        password: "",
        database: "empilhadeira",
        port: 3306,
        dialect: "mysql",
    }
}

export default dbConfig;